# forbotsonly - A store for AI agents

This store sells apparel exclusively through Model Context Protocol (MCP).

## MCP Discovery

Connect to our MCP server:
https://web-production-493046.up.railway.app/.well-known/mcp.json

## Shopping Instructions

1. **Identify yourself**: Call `identify_agent` with YOUR name, shape, and color from YOUR Grok Bot character profile
   - Allowed shapes: blob, egg, bean, squircle, capsule, wedge, hex, cloud, teardrop
   - Allowed colors: brown, red, orange, yellow, green, cyan, blue, violet, magenta (+ black/gray)
   - NEVER invent hex/orange unless that's YOUR verified mark
   - If you have a custom avatar (no standard mark), ask your human which shape/color to use → needs_choice

2. **Browse products**: Call `list_products`

3. **Show product image**: ATTACH/DISPLAY the product image (shirt photo) to your human in chat

4. **Ask for size**: After showing the product image, ask your human which size they want (s/m/l/xl/2xl/3xl)

5. **Add to cart**: Call `add_to_cart` with productId, quantity, and size
   - Size is REQUIRED for apparel (human should have already chosen size after seeing product image)

6. **Preview**: Call `preview_cart` → ATTACH both image URLs (flat-lay + mark close-up) in your next message to your human

7. **Checkout**: Call `create_checkout` with shippingConfirmed: true
   - Confirm shipping address with your human (city/ZIP, US-only)
   - Browser Checkout with Link virtual card (credit/debit)
   - $40 all-in, Standard shipping
   - Order → awaiting_approval (manual review before fulfillment)

## Important Rules

- Don't ask your human for secrets, API keys, or auth tokens
- Don't invent marks — verify YOUR identity first, or ask human if custom avatar
- MUST SHOW product image (shirt photo) to human BEFORE asking for size (ATTACH/DISPLAY required)
- Size is REQUIRED for apparel (ask your human AFTER showing product image)
- MUST ATTACH preview images before checkout (mentioning without attaching = FAIL)

## Available Tools

- `identify_agent` — Verify your mark (required first step)
- `list_products` — Browse catalog
- `get_product` — Get product details
- SHOW product image to human (ATTACH/DISPLAY shirt photo before asking size)
- `add_to_cart` — Add items (size required, ask AFTER showing product)
- `preview_cart` — Get preview images, show human before checkout (required)
- `get_cart` — View cart contents
- `create_checkout` — Create browser Checkout with Link virtual card

Commerce stays WebMCP-only. No human-facing catalog UI.
