{
  "name": "forbotsonly",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "bun run src/server.ts",
    "prodigi:ping": "bun run scripts/ping.ts"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^6.0.0"
  }
}
