{
  "name": "forbotsonly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/images && cp -R public/images/* dist/images/ || true",
    "preview": "vite preview",
    "server": "bun run src/server.ts",
    "prodigi:ping": "bun run scripts/ping.ts",
    "marks:generate": "bun run scripts/generate-marks.ts"
  },
  "dependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "stripe": "^17.5.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/three": "^0.170.0",
    "sharp": "^0.35.4",
    "typescript": "^7.0.2",
    "vite": "^6.0.0"
  }
}
